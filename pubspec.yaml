name: async_confirm_dialog
description: >-
  A zero-boilerplate, async-aware confirmation dialog for Flutter. Handles
  loading states, double-tap prevention, and errors natively with adaptive
  Material and Cupertino designs.
version: 1.0.1
repository: https://github.com/eebrahimjoy/async_confirm_dialog
issue_tracker: https://github.com/eebrahimjoy/async_confirm_dialog/issues
homepage: https://github.com/eebrahimjoy/async_confirm_dialog

topics:
  - dialog
  - confirmation
  - async
  - material
  - cupertino

environment:
  sdk: ^3.12.2
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
