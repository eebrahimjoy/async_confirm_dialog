# Files excluded from the published package (dart pub publish uses .pubignore,
# not .gitignore, to decide what to ship).

# Demo GIFs are used only by the README (via GitHub raw URLs) and the example
# app, so consumers should not download ~40 MB on `flutter pub get`.
assets/

# Build caches and generated artifacts.
build/
.dart_tool/